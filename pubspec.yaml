name: mobile_helper
description: "A new Flutter package project."
version: 0.0.2
homepage: https://github.com/TunaiPackages/mobile_helper.git

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  equatable: ^2.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  test: ^1.25.7
